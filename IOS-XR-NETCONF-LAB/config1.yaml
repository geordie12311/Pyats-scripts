---
#example configuration file for nornir
inventory:
   plugin: SimpleInventory
   options:
       host_file: "hosts1.yaml"
       group_file: "groups.yaml"
       defaults_file: "defaults.yaml"

runner: 
    plugin: threaded
    options:
        num_workers: 50
